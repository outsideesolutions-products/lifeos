{
  "name": "@lifeos/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema and generated client — shared database access layer for LifeOS. Milestone 0 scope: Secrets Management and Trust & Authorization tables only. Domain object tables land starting Milestone 1, gated on resolving Blocking findings B1/B2 in docs/architecture-compliance-report.md.",
  "main": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.6.0"
  }
}
