{
  "name": "@lifeos/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema and generated client — shared database access layer for LifeOS. Milestone 0 tables (Secrets Management, Trust & Authorization) plus Milestone 1's Identity/Core Object/Constitution foundation. See schema.prisma's file header for details.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "prisma db seed",
    "studio": "prisma studio",
    "test": "jest"
  },
  "dependencies": {
    "@lifeos/domain-model": "workspace:*",
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.0",
    "jest": "^29.7.0",
    "prisma": "^5.20.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
